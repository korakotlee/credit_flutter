import 'package:flutter/material.dart';
import 'package:credit_flutter/ui/listview_note.dart';

void main() => runApp(
      MaterialApp(
        title: 'Returning Data',
        home: ListViewNote(),
      ),
    );
